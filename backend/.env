# .env — Environment Variables
# These are secrets and settings that change between environments
# (development on your laptop vs production on a server)
# NEVER commit this file to GitHub — it contains secrets

PORT=3000
NODE_ENV=development